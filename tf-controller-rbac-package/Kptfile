apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: tf-controller-rbac
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: this package is used to create the necessary RBAC for the Flux TF controller to get needed Github and Cluster kubeconfig and tokens