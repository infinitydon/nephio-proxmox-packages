apiVersion: config.porch.kpt.dev/v1alpha1
kind: PackageVariant
metadata:
  name: metallb
spec:
  upstream:
    repo: nephio-proxmox-packages
    package: metallb-package
    revision: r3
  downstream:
    repo: mgmt
    package: metallb-package
  annotations:
    approval.nephio.org/policy: initial